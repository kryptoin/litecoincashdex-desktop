# ---------------------------------------------------------
# Core build directories (generated by your custom script)
# ---------------------------------------------------------
/build-macos-anaconda3
/build-macos-anaconda3-test
/build-macos-analysis
/build-macos-apple-silicon
/libwally-core-install

# ---------------------------------------------------------
# Manually downloaded dependency (KEEP)
# ---------------------------------------------------------
# build-macos-lwc-r1.5.6 is intentionally NOT ignored

# ---------------------------------------------------------
# Local environment / temporary dirs
# ---------------------------------------------------------
/dist
/local
/wally-install

# ---------------------------------------------------------
# Fetched / generated artifacts (do NOT commit)
# ---------------------------------------------------------
# Persistent FetchContent cache (qmaterial git clone reused across builds)
/.fetchcontent-cache
# Downloaded KDF daemon binary (populated by build script)
/assets/tools/kdf/mm2_cheetah
# libwally-core build tree
/libwally-core-build
# vendor/coins is a git submodule (GLEECBTC/coins) — ignore its checkout
/vendor/coins
/build-macos-lwc-r1.5.6

# ---------------------------------------------------------
# CMake + Ninja build artifacts
# ---------------------------------------------------------
CMakeFiles/
/CMakeCache.txt
cmake_install.cmake
*.ninja
.ninja_deps
.ninja_log

# ---------------------------------------------------------
# Logs, dumps, and crash artifacts
# ---------------------------------------------------------
*.log
*.dump
backtrace.dump

# ---------------------------------------------------------
# Python caches
# ---------------------------------------------------------
__pycache__/
*.pyc
*.pyo

# ---------------------------------------------------------
# Build related
# ---------------------------------------------------------
#package-macos.sh

# ---------------------------------------------------------
# Compiled objects and libraries
# ---------------------------------------------------------
*.o
*.obj
*.a
*.so
*.dylib
*.dll
*.exe

# ---------------------------------------------------------
# Misc build metadata
# ---------------------------------------------------------
*.yaml
*.tmp
*.cache

# ---------------------------------------------------------
# macOS / editor noise
# ---------------------------------------------------------
.DS_Store
.idea/
.vscode/

# ---------------------------------------------------------
# Documentation
# ---------------------------------------------------------

# Ignore all markdown files inside /docs
/docs/**/*.md
/docs/*.md

# But allow the images folder
!/docs/images/